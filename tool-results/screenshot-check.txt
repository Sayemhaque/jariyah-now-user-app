SCREENSHOT ANALYSIS — pasted_image_1783266266134.png (815×441 RGBA)
=====================================================================
Source: z-ai vision CLI (glm-4.6v) — two passes
Files : screenshot-check-raw.json, screenshot-check-detail.json

---------------------------------------------------------------------
1) LIVE IN-BROWSER PREVIEW  vs.  EXPORT MODAL (rendered MP4 preview)
---------------------------------------------------------------------
This is the **LIVE in-browser preview**, NOT the export-modal MP4
preview.

Evidence:
  • Background is a **mountain landscape** (rugged peaks at sunrise/sunset
    with clouds/fog) — this matches the `mountain.png` option selectable
    in CustomizationPanel, NOT the default `twilight-mosque` brand theme.
  • A solid "JARIYAH NOW" text label sits at the **bottom-right** corner
    of the video frame. That element only exists in `VideoPreview.tsx`
    (lines 572–583) — the live preview's text watermark. The export
    modal (`ExportModal.tsx` drawFrame) does NOT render a bottom-right
    "Jariyah Now" string; it only renders the top-center PNG watermark
    (or its "Made with Jariyah Now" text fallback).
  • The frame composition (gradient header bar with surah name +
    ayat indicator at the top, attribution lines at bottom-left) matches
    the live preview overlay structure.

---------------------------------------------------------------------
2) IS THERE A WATERMARK VISIBLE ANYWHERE?
---------------------------------------------------------------------
  • Bottom-right:  YES — "JARIYAH NOW" text label (uppercase, tracked).
    VLM perceives it as solid/opaque. Source code says it is rendered
    with `text-white/50` (50% opacity white) in a `font-mono` face,
    which is subtle on a bright mountain sky. So it IS rendered, but
    its low opacity on a light background can make it look faded.
  • Top-center:    NO — the PNG watermark (`/public/watermark.png`,
    the crescent + play + diamond brand mark) is NOT present.
    This is expected, because the live `VideoPreview.tsx` does not
    draw the PNG watermark at all — only the export pipeline does
    (`ExportModal.tsx` lines 1352–1390).
  • Top-right, top-left, bottom-left: no additional watermark.

---------------------------------------------------------------------
3) TEXT / ELEMENTS VISIBLE AT THE TOP OF THE VIDEO FRAME
---------------------------------------------------------------------
  • Top-left:  Arabic "بِسْمِ اللَّهِ" (Bismillah)
  • Below it:  "AAL-E-IMRAN · MEDINAN" (surah name + revelation type)
  • Top-right: "3:1" (surah:ayah), and below it "AYAT 1 OF 3"
  • Top-center:  (empty) — no crescent/PNG watermark, no
    "Made with Jariyah Now" fallback text.

---------------------------------------------------------------------
4) TEXT / ELEMENTS VISIBLE AT THE BOTTOM OF THE VIDEO FRAME
---------------------------------------------------------------------
  • Bottom-left (two lines, attribution block):
      - Translation credit:
          "Translation: আল্লাহ তাআলার নামে — মুহিউদ্দিন খান
           (Muhuiduddin Khan)"
      - "Recited by Mishary Alafasy"
  • Bottom-right:  "JARIYAH NOW" — the live-preview text watermark.

---------------------------------------------------------------------
INTERPRETATION OF THE USER'S COMPLAINT
---------------------------------------------------------------------
The user says "I cannot see the watermark in the video preview."

Two plausible readings, given what is actually on screen:

  (a) They are looking at THIS live preview and expecting the
      crescent-shaped PNG brand mark at TOP-CENTER. That mark is
      intentionally NOT drawn by `VideoPreview.tsx` — it is only
      composited into the exported MP4 by `ExportModal.tsx drawFrame`.
      So on the live preview the top-center watermark is absent by
      design, and only the faint "Jariyah Now" text sits at the
      bottom-right.

  (b) They consider the bottom-right "Jariyah Now" text (50% opacity
      white on a bright mountain sky) too faint to read, so they
      report it as "missing." This is a contrast/opacity issue, not
      a missing-render issue.

To confirm which: ask the user to open the EXPORT MODAL and let the
rendered MP4 play — the top-center PNG watermark should then appear.
If they already opened the export modal and STILL don't see it, the
bug is in `ExportModal.tsx` (watermarkImg load failure → text
fallback not firing, or `drawImage` skipping on first frame).
